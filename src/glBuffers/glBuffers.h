#ifndef GLBUFFERS_H
#define GLBUFFERS_H

#include <glad/glad.h>

struct Rendering {
  GLuint VAO_ID;
  GLuint VBO_ID;
  GLuint IBO_ID;
// Shader
};

struct PostProccesing {
  GLuint RBO_ID;
  GLuint FBO_ID;
// Shader
};

struct Shader {
// to do
};

#endif